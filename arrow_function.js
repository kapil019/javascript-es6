/*
  Arrow functions allows a short syntax for writing function expressions.
  You don't need the function keyword, the return keyword, and the curly brackets.
*/
// Example
const x = (x, y) => x * y;

// Call function
console.log("Product of 2 and 3 is ", x(2,3));
